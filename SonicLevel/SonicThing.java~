/*Credit to: Bailey, Dinith, and William
 *Created: June 15, 2015
 *Last Modified: June 15, 2015
 *Description: Customized thing for the SonicLevel
 */

import becker.robots.icons.*;
import java.awt.Color;
import java.awt.*;

//This code is used create a new icon for the SonicLevel
public class SonicThing extends Icon
{
  public SonicThing()
  {
    super();
  }
  
  public void paintIcon(Graphics g)
  {
    //Make outer yellow ring
    g.setColor(Color.YELLOW);
    g.fillOval(30, 30, 40, 40);
    
    //Make inner white space
    g.setColor(Color.WHITE);
    g.fillOval(40, 40, 20, 20);
  }
}
